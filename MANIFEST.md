# Repository manifest

This repo is a GitHub wrapper around the MDAB v0.1 Telemetry CTS bundle.

- `telemetry_cts/` contains the normative schema, requirements/traceability, chain verifier, fixtures, and keys.
- `appendix/` contains Appendix A (how to claim conformance) and the decision_hash patch.

For integrity and offline evidence, see `telemetry_cts/SHA256SUMS.txt`.
