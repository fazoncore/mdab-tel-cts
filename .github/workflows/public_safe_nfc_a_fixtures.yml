name: public-safe-nfc-a-fixtures
on:
  push:
  pull_request:

jobs:
  verify:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
      - name: Verify PUBLIC_SAFE_NFC_A fixtures
        run: |
          python3 scripts/verify_public_safe_nfc_a_fixtures.py
